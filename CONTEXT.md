# 上毛かるたタイピング

上毛かるた44札の読みをローマ字でタイピングするゲーム。本ドキュメントはこのプロジェクト固有の用語集であり、実装の詳細や仕様書ではない。

## Language

### 入力判定

**撥音ルール**:
撥音「ん」のローマ字許容を文脈で決める規則。次の音が母音・や行・な行のとき、および語末では `nn` のみを正解とし、それ以外では `n` / `nn` の双方を許容する。例: `ぐんまけん` → `gunmakenn`（`gunmaken` は不正解）。
_Avoid_: nルール、撥音処理

**打鍵途中**:
現在の仮名単位のローマ字を打ち始めたが、まだ打ち終えていない状態。表示上はその単位を `current` として示す。撥音ルールが働く「ん」の完成待ちもこれに含む。
_Avoid_: 部分入力（削除した未接続モードの旧称）、入力途中、未確定（IME 用語との混同を避ける）

**短縮読み**:
初心者難易度で読み札の代わりに出題する、札ごとにあらかじめ用意された短い読み。全44札に1つずつ定義されている。読みの先頭を機械的に切り出したものではなく、長さも札ごとに異なる。
_Avoid_: 部分入力、一部入力、部分タイピング、短縮ワード

### 表示モード

**ブラインド入力**:
未入力部分を伏せ字（`_`）で隠して打鍵させる表示モード。Enterキーで読み札全文をヒント表示できる。
_Avoid_: 目隠しモード、ブラインドモード

**開始カウントダウン**:
ゲーム開始前に挟む約3.8秒のカウントダウン演出。再開カウントダウンとは別物。
_Avoid_: スタートカウント、初回カウントダウン、（無修飾の）カウントダウン

**再開カウントダウン**:
ポーズ解除からゲーム再開までに挟む約3.8秒のカウントダウン演出。開始カウントダウンとは別物。
_Avoid_: リスタートカウント、再開タイマー、（無修飾の）カウントダウン

### アカウントと設定

**ゲスト**:
未ログインのプレイヤー。ユーザー設定は端末ごとに保存され、端末を跨がない。
_Avoid_: 匿名ユーザー、非会員

**ユーザー設定**:
設定画面で変更できるプレイヤーごとの好み（表示・サウンド・キーボード）。設定画面に露出している項目のみを指し、ゲームモードや難易度などプレイごとの選択は含まない。
_Avoid_: ゲーム設定、環境設定

**アカウント優先**:
ログイン中はアカウントに保存されたユーザー設定が端末に残る設定より常に勝つ規則。唯一の例外は初回引き継ぎ。
_Avoid_: サーバー優先、マージ

**初回引き継ぎ**:
初めてログインしたとき一度だけ、ゲスト時代のユーザー設定をアカウントへ取り込むこと。
_Avoid_: （無修飾の）同期、インポート

### スコア表示

**ライブスコア**:
プレイ中（開始カウントダウン後）に常時表示するスコア指標。正確率・コンボ・スコアの3つで、WPM と最大コンボは含まない（それらは最終スコアでのみ出す）。
_Avoid_: リアルタイムスコア、ライブWPM（WPM はライブには存在しない）

**最終スコア**:
ゲーム終了画面でのみ表示する集計結果。正解札数・正確率・WPM・最大コンボを含む。プレイ中のライブスコアとは別物。
_Avoid_: リザルト、結果スコア、（ライブスコアと混同した）ライブWPM

**ランキング登録**:
ゲーム終了後に、任意のニックネームを付けてスコアを公開ランキングへ送ること。任意かつ明示的な操作であり、登録しなかったプレイは記録に残らない。
_Avoid_: スコア送信、スコア保存

**プレイ記録**:
ログインユーザーがゲームを自然完了したとき自動で残る1プレイ分の記録。モード・難易度・スコア・タイム・正確率・最大コンボ・プレイ日時を持つ。ランキング登録とは独立し、登録の有無に関わらず完了プレイすべてが残る。中断・離脱では残らず、ゲストにも残らない。
_Avoid_: ゲーム履歴、プレイログ、プレイ履歴（履歴は記録を一覧する画面の呼称）

**ベストスコア**:
ログインユーザーの自己最高記録。全プレイ記録が対象で、ランキング登録の有無を問わない。モードごとに1つ（ランダム＝スコア最大、タイムアタック＝タイム最小）で、達成した難易度を添えて示す。ゲストには存在しない。
_Avoid_: 自己ベスト、ハイスコア、最高記録

### バッジ

**バッジ**:
達成条件を満たすと解除される称号。名称・説明・カテゴリ・達成条件を持ち、解除済みは解除日時を伴う。「実績」はバッジと同義の別概念ではなく、画面名・メニュー表記「実績・バッジ」の中でのみ現れる。
_Avoid_: 実績（単体では使わない）、アチーブメント、トロフィー、称号

**連続プレイ**:
日本時間の暦日で数え、プレイ記録のある日が途切れず続いている日数。今日まだプレイしていなくても昨日までの連続は生きており、暦日がプレイ無しで終わって初めて途切れる。継続系バッジの判定は現在の連続日数ではなく史上最大の連続日数で行う。
_Avoid_: ストリーク、連続ログイン（ログインではなくプレイで数える）、連続プレイ日数（パネル表記は「連続プレイ」）

### フィードバック

**フィードバック**:
プレイヤーがアプリ内フォームから運営へ送る連絡。バグ報告・機能リクエスト・使い方の質問・その他 の4種類を、種類を選んで1つのフォームで送る単一チャネル。本文が主で、件名と返信先メールアドレスは任意。ゲストも送信でき、送信内容は公開されず、他のプレイヤーからは見えない。
_Avoid_: お問い合わせ、問い合わせ、バグ報告（単体では4種類のうちの1つを指すだけ）

### 対戦

**対戦モード**:
ログインユーザー2人が合言葉でマッチングし、同一の山札を個別同時プレイで打ち合う対人戦。既存のランダム・タイムアタックそれぞれに対戦版があり、難易度は標準固定。リザルトはその場限りで、プレイ記録・ランキングには残らない。
_Avoid_: マルチプレイ、対人戦、バトル

**ルーム**:
対戦のマッチング単位。モードと合言葉の組で識別され、作成した側（ホスト）が待機し、2人目（ゲスト）の参加で成立する。成立した瞬間にサーバが山札を確定し、両者へ同一順序で配る。
_Avoid_: マッチ、ロビー、部屋（ドキュメント・issue・コードでは避ける。プレイヤー向け画面文言のみ design.pen に合わせて「部屋」と表記する）

**合言葉**:
ルームを見つけ合うための1〜20文字の合図。友達同士で口頭やチャットで共有する前提で、秘密のパスワードではない。モードと合言葉の両方が一致した相手とだけマッチングする。
_Avoid_: パスワード、ルームコード、あいことば（表記は漢字）

**マッチング**:
合言葉を送ってから対戦相手が決まるまでの待機・成立の過程。「合言葉を設定」「合言葉を入れる」は表現の違いだけで、どちらも「待機中のルームがあれば参加、なければ作成」で動き、押した順序に関わらず必ず繋がる。
_Avoid_: ロビー、ペアリング、募集

**個別同時プレイ**:
対戦のルール。両者が同一の山札・同一順序を各自のペースで打ち、リアルタイムに共有されるのは相手の取得札数のみで、札の取り合いはしない。ランダム＝60秒でスコアが高い方が勝ち（同点は引き分け）、タイムアタック＝先に自分の10枚を打ち切った方が勝ち。対戦タイムアタックにスキップはない。
_Avoid_: 同期プレイ、早取り、取り合い

**不戦勝**:
決着前に相手が離脱（退出・タブ閉じ・リロード・回線断）した、または自分の終了後に相手の結果が届かないまま待機が切れたときの勝ち扱い。
_Avoid_: ウォークオーバー（コード内の識別子 walkover は可、表示では使わない）、没収勝ち
